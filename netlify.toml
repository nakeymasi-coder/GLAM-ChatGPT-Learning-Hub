[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Optional: change the coaching model without editing code.
# Add OPENAI_COACH_MODEL in Netlify Environment Variables if desired.
